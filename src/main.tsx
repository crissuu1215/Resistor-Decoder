import React from "react";
import ReactDOM from "react-dom/client";
import ResistorApp from "./App.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ResistorApp />
  </React.StrictMode>
);
